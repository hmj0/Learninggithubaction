name: first_pipeline
permissions:
  contents: read

on:
  push:
    branches: [main]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [main]
    paths-ignore:
      - '**/*.md'
  


jobs:
  message:
    runs-on: ubuntu-22.04

    steps:
      - name: first message
        run: echo "hello world"